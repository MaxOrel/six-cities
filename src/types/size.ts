export type TSizeMap = {
  small: { width: string; height: string };
  large: { width: string; height: string };
};

export type TSizeBookmark = {
  small: { width: string; height: string };
  large: { width: string; height: string };
};
